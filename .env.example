# Template for .env.local - Copy this to .env.local and fill in values

# Database - Neon PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# Auth.js
AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_TRUST_HOST=true

# Google OAuth (configure at https://console.cloud.google.com/apis/credentials)
# AUTH_GOOGLE_ID="your-google-client-id"
# AUTH_GOOGLE_SECRET="your-google-client-secret"

# Base path for GitHub Pages (keep for static export compatibility)
NEXT_PUBLIC_BASE_PATH="/Appagar"
