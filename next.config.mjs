/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // Configurado para GitHub Pages
  basePath: '/Appagar',
};

export default nextConfig;
